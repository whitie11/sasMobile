<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="SASMobileApp1.Views.HomeMDPage">

    <MasterDetailPage.Master>
        <ContentPage Title="Menu">
            <StackLayout Padding="20">

                <Label Text="HomeMD Page" />
                <!-- TODO: // Update the Layout and add some real menu items  -->
                <Button Text="About" Command="{Binding NavigateCommand}" CommandParameter="AboutPage" />
                <Button Text="Patients" Command="{Binding NavigateCommand}" CommandParameter="PatientListPage" />
                <Button Text="Environmental Checks" Command="{Binding NavigateCommand}" CommandParameter="EnviromentalPage" />
                <Button Text="Admin" Command="{Binding NavigateCommand}" CommandParameter="AdminPage" />
            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>
    
</MasterDetailPage>